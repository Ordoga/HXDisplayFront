export type User = {
    name: string
    email: string
}

export type Product = {
    id: number
    name: string
    description: string
    price: number
    imageUrl: string
    category: string
    brand: string
}
